import React from 'react';

interface LayoutProps {
}

const Layout: React.FC<LayoutProps> = ({  }) => {
  return (
    <div>
      <h1>Hello!</h1>
      <p>Ovo je moja prva React komponenta.</p>
    </div>
  );
};

export default Layout;
